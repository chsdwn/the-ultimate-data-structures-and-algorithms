using System;

namespace the_ultimate_data_structures_and_algorithms
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] numbers = { 1, 2, 3 };

            // Constant     O(1)
            // Logarithmic  O(log n)
            // Linear       O(n)
            // Quadratic    O(n^2)
            // Exponential  O(2^n)
        }
    }
}